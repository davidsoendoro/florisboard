<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kokatto.kobold">
    <!-- Permission needed to vibrate if the user has key press vibration enabled -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="dev.patrickgold.florisboard.FlorisApplication"
        android:allowBackup="false"
        android:icon="@mipmap/floris_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/floris_app_icon_round"
        android:supportsRtl="true"
        android:theme="@style/SettingsTheme">
        <activity
            android:name=".dashboardcreatetransaction.SearchTransactionActivity"
            android:exported="true" />
        <activity
            android:name=".dashboardcreatetransaction.ArchiveActivity"
            android:exported="true" /> <!-- IME service -->
        <service
            android:name="dev.patrickgold.florisboard.FlorisImeService"
            android:directBootAware="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>

            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service> <!-- Spellchecker service -->
        <service
            android:name="dev.patrickgold.florisboard.FlorisSpellCheckerService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_TEXT_SERVICE">
            <intent-filter>
                <action android:name="android.service.textservice.SpellCheckerService" />
            </intent-filter>

            <meta-data
                android:name="android.view.textservice.scs"
                android:resource="@xml/spellchecker" />
        </service> <!-- Settings Activity -->
        <activity
            android:name="dev.patrickgold.florisboard.settings.SettingsMainActivity"
            android:icon="@mipmap/floris_app_icon"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:roundIcon="@mipmap/floris_app_icon_round"
            android:theme="@style/SettingsTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".template.TemplateActivity"
            android:icon="@mipmap/floris_app_icon"
            android:label="Kobold Dashboard Template"
            android:launchMode="singleTask"
            android:roundIcon="@mipmap/floris_app_icon_round" >
        </activity>
        <activity
            android:name=".template.TemplateActivityInput"
            android:icon="@mipmap/floris_app_icon"
            android:label="Kobold Dashboard"
            android:roundIcon="@mipmap/floris_app_icon_round" />
        <activity
            android:name=".template.TemplateActivitySearch"
            android:icon="@mipmap/floris_app_icon"
            android:label="Kobold Dashboard"
            android:roundIcon="@mipmap/floris_app_icon_round" /> <!-- User Dictionary Manager Activity -->
        <activity
            android:name="dev.patrickgold.florisboard.settings.UdmActivity"
            android:icon="@mipmap/floris_app_icon"
            android:label="@string/settings__title"
            android:theme="@style/SettingsTheme" /> <!-- Theme Selector Activity -->
        <activity
            android:name="dev.patrickgold.florisboard.settings.ThemeManagerActivity"
            android:icon="@mipmap/floris_app_icon"
            android:label="@string/settings__title"
            android:theme="@style/SettingsTheme" /> <!-- Theme Editor Activity -->
        <activity
            android:name="dev.patrickgold.florisboard.settings.ThemeEditorActivity"
            android:icon="@mipmap/floris_app_icon"
            android:label="@string/settings__theme_editor__title"
            android:theme="@style/SettingsTheme" /> <!-- Spelling Activity -->
        <activity
            android:name="dev.patrickgold.florisboard.settings.spelling.SpellingActivity"
            android:icon="@mipmap/floris_app_icon"
            android:label="@string/settings__spelling__title_overview"
            android:roundIcon="@mipmap/floris_app_icon_round"
            android:theme="@style/SettingsTheme" /> <!-- About Activity -->
        <activity
            android:name="dev.patrickgold.florisboard.settings.AboutActivity"
            android:icon="@mipmap/floris_app_icon"
            android:label="@string/about__title"
            android:roundIcon="@mipmap/floris_app_icon_round"
            android:theme="@style/SettingsTheme" /> <!-- Advanced Activity -->
        <activity
            android:name="dev.patrickgold.florisboard.settings.AdvancedActivity"
            android:icon="@mipmap/floris_app_icon"
            android:label="@string/settings__advanced__title"
            android:roundIcon="@mipmap/floris_app_icon_round"
            android:theme="@style/SettingsTheme" /> <!-- Setup Activity -->
        <activity
            android:name="dev.patrickgold.florisboard.setup.SetupActivity"
            android:icon="@mipmap/floris_app_icon"
            android:label="@string/setup__title"
            android:roundIcon="@mipmap/floris_app_icon_round"
            android:theme="@style/SettingsTheme" />

        <activity-alias
            android:name="dev.patrickgold.florisboard.SettingsLauncherAlias"
            android:icon="@mipmap/floris_app_icon"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:roundIcon="@mipmap/floris_app_icon_round"
            android:targetActivity="dev.patrickgold.florisboard.setup.SetupActivity" /> <!-- Using an activity alias to disable/enable the app icon in the launcher -->
        <!-- Dashboard Transaction Activity -->
        <activity
            android:name=".dashboardcreatetransaction.CreateTransactionActivity"
            android:exported="true"
            android:label="Kobold Dashboard Transaction">


        </activity>
        <activity
            android:name=".dashboardcreatetransaction.InputActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".dashboardcreatetransaction.DetailActivity"
            android:exported="true">
        </activity>
        <!-- Dashboard Shipping Cost  -->
        <activity
            android:name=".dashboardcheckshippingcost.CheckShippingcost"
            android:exported="true">

        </activity>
        <activity
            android:name=".dashboardcheckshippingcost.AddressShippingActivity"
            android:exported="true"
            android:windowSoftInputMode="stateVisible"/>
        <!-- Dashboard Bank Management Activity -->
        <activity
            android:name=".bank.BankHomeActivity"
            android:exported="true"/>
        <activity
            android:name=".bank.BankInputActivity"
            android:exported="true"/>
        <!-- Crash Dialog Activity -->
        <activity
            android:name="dev.patrickgold.florisboard.crashutility.CrashDialogActivity"
            android:icon="@mipmap/floris_app_icon"
            android:label="@string/crash_dialog__title"
            android:theme="@style/CrashDialogTheme" />

        <provider
            android:name="dev.patrickgold.florisboard.ime.clip.provider.FlorisContentProvider"
            android:authorities="${applicationId}.provider.clip"
            android:exported="false"
            android:grantUriPermissions="true" />
    </application>

</manifest>
